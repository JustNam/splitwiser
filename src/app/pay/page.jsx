// STUB — replaced in Step 9. Route: /pay
export default function PayPage() {
  return (
    <main style={{ padding: 'var(--space-16)' }}>
      <h1>B5 · Pay &amp; debts</h1>
      <p>
        Every debt listed separately — who, which session, which cost line, how
        much — grouped by person. Each one is paid in full; there is no amount
        input anywhere on this screen. The confirm step is mandatory, because
        there is no undo.
      </p>
    </main>
  )
}

// STUB — replaced in Step 10. Route: /group/new
export default function NewGroupPage() {
  return (
    <main style={{ padding: 'var(--space-16)' }}>
      <h1>A4 · New group</h1>
      <p>
        Name the group, then show the invite code immediately and say what to
        do with it (&ldquo;send this link to the group chat&rdquo;). That reveal
        is A4b, and it is a state of this screen, not a separate route — it is
        the only path to a second member.
      </p>
    </main>
  )
}

// STUB — replaced in Step 7. Route: /sessions/new
export default function NewSessionPage() {
  return (
    <main style={{ padding: 'var(--space-16)' }}>
      <h1>B2 · New session</h1>
      <p>
        The big one. Date, cost lines (one by default, each with its own
        payer), who played, and the split — with &ldquo;remaining to
        allocate&rdquo; live, and Save blocked while it is not zero.
      </p>
    </main>
  )
}
